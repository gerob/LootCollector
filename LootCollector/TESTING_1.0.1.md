# 1.0.1 tester checklist (Discord)

Copy everything inside the box below into Discord.

```
LootCollector 1.0.1 — quick test
Install 1.0.1 → type /reload → note your realm.

Reply with Pass / Fail / Skipped for each section.

0) Version
Open Discoveries, hover version (top-left). Should say Beta-1.0.1.

1) Search chips
• Search row = one box + Add (no Deep Search checkbox)
• Type something (e.g. Fenris) → Add/Enter → chip appears, list shrinks
• Open Filters (N) → can remove chip with X
• Add a 2nd chip (e.g. stamina) → list shrinks more
• Try: stamina AND agility → still works
• Clear → list back to normal

2) Filter Map
• Set a filter in Discoveries → click Filter Map (On / gold)
• Map/minimap pins should match the filtered list
• Turn Filter Map Off → pins back to normal
• Optional: map menu has Filter Map + Clear Discoveries Filters (no Show Zone Summary)

3) Enchant filter (was Collected)
• Bronzebeard / Area 52: Enchant button between Looted and Date
• Rexxar / Vol'jin (CoA): Enchant button HIDDEN
• If you see it: click All → Yes → No → All (list changes, no errors)

4) Pause
• /lcpause → says paused
• Play 1–2 min → feels normal
• /lcpause again → resumes

5) Login / map (quick)
• /reload or login → map/minimap not endlessly blank/flickering
• Change zones with map open → pins update, no long freeze
• Alt+Tab out and back → no long hang

Optional: Worldforged phase right-click still works; Vendors tab opens.

Send back:
Realm:
Version OK? (yes/no)
1 Search:
2 Filter Map:
3 Enchant:
4 Pause:
5 Login/map:
Notes / fails:
```
